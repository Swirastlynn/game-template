name: game_template
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  # A single Dart SDK can simultaneously support multiple versions of the Dart language. 
  # The compiler determines what version the code is targeting, and it interprets the code according to that version.
  sdk: '>=2.18.5 <3.0.0' # dart

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^3.0.1
  device_preview: ^1.1.0
  equatable: ^2.0.5

  # architecture
  bloc: 8.0.3
  flutter_bloc: 8.0.1

  # localization
  flutter_localizations:
    sdk: flutter
  intl: 0.17.0

  # dependency injection
  get_it: ^7.2.0

dev_dependencies:
  # tests
  test: ^1.22.2
  mocktail: ^0.3.0
  bloc_test: 9.0.3

  # code analysis
  flutter_lints: ^2.0.1
  dart_code_metrics: ^5.5.0

flutter:
  uses-material-design: true

  # localization
  generate: true